# Assignment 10 — Classification Algorithms Comparison (Diabetes)

Summary
- Compares several classification algorithms (SVM, Naive Bayes, Decision Tree, KNN) on the Pima Indians Diabetes dataset. Covers preprocessing, scaling, training multiple models, and comparing accuracy/precision/recall/F1.

Dataset
- `pima-indians-diabetes.data.csv` — columns (used in notebook): Pregnancies, Glucose, BloodPressure, SkinThickness, Insulin, BMI, DiabetesPedigreeFunction, Age, Outcome

How to run
- Open `model.ipynb`. Requires pandas, numpy, scikit-learn, seaborn, matplotlib.

Notes
- This notebook uses sklearn implementations (no TensorFlow required).
